cat > package.json << 'EOF'
{
  "name": "bot-discord",
  "version": "2.0.0",
  "description": "Discord bot with casino games",
  "main": "discord.js",
  "scripts": {
    "start": "node discord.js",
    "deploy": "node deploy-commands.js"
  },
  "dependencies": {
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "19.x"
  }
}
EOF
